# Nerq Bluesky Post Drafts

## 1. Browser Extension

Nerq now shows trust badges directly on GitHub, npm, and PyPI pages. Before you install a package or clone a repo, see its Trust Score inline — computed from 5M+ indexed AI assets. No context switching. Install the extension and stop guessing.

nerq.ai

#DevTools #AISupplyChain #Security

## 2. VS Code Extension

Inline trust scores in your editor. The Nerq VS Code extension surfaces Trust Scores next to every dependency in your package.json, requirements.txt, or go.mod. Flag risky AI packages before they hit your lock file.

nerq.ai

#DevTools #OpenSource #Security

## 3. GitHub App

New: Nerq GitHub App adds dependency trust reports to every PR. It scans added or updated packages against 5M+ indexed AI assets and posts a Trust Score summary as a PR check. Catch supply chain risk before you merge.

nerq.ai

#AISupplyChain #DevTools #OpenSource

## 4. MCP Server

Query Nerq trust data from Claude or Cursor via MCP. Ask "is this agent safe?" and get a structured Trust Score response backed by 5M+ indexed assets. One tool call, no browser needed. Docs at nerq.ai/docs/mcp

nerq.ai

#DevTools #AISupplyChain #Security

## 5. API — /v1/preflight

Ship a preflight check for AI dependencies. POST to nerq.ai/v1/preflight with a list of packages and get back Trust Scores, risk flags, and a pass/fail verdict. Drop it into CI or call it from your agent runtime.

nerq.ai

#DevTools #AISupplyChain #OpenSource
